# PRD:vibecoding-retro 工作流优化复盘 Skill

版本 v2.1 · 2026-07-15 · 状态:已实现,待实测校准
v2.1 核心变更:目标用户改为"高频使用 AI 的产品经理",姿态从成长教练改为专业同侪;新增外源知识库与"推荐→实践→验证"学习闭环;北极星改为建议实践率。

## 一、定位与问题

**一句话定位**:帮高频使用 AI 的产品经理,把每次 vibecoding 会话变成一次有出处、可验证的工作流优化。

vibecoding 会话是一个未被利用的富矿:用户在其中反复暴露自己的思维方式(如何定义问题、如何控制范围、如何与 AI 分工)和技术盲区(缺什么概念、能否评估方案),但没有任何工具从"人的成长"角度挖掘这些数据。现有工具的断层:ccusage/token-dashboard 类只做量化仪表盘;claude-retrospective/claude-improve 类只复盘项目和配置,不复盘人。**没有产品回答"这次会话说明我这个人缺什么"。**

两类指标性质不同,产品设计上区别对待:效率是运营指标,适合打分;能力缺口是成长指标,单次证据薄,适合"假设式诊断 + 跨会话累积台账",反复出现才定性为结构性问题。

## 二、目标与非目标

目标:每次会话后 2 分钟内产出以能力诊断为正文的复盘报告;通过缺口台账让用户看见自己的成长轨迹(缺口出现 → 确认 → 改善 → 毕业);4–8 周内使用户在问题定义、范围控制、人机分工等 PM 核心能力上出现可观测改善。

非目标:团队对比与排行;实时监控;心理分析或人格评判(只诊断行为模式);自动修改用户配置;Cursor 等封闭环境的 token 自动抓取。

## 三、功能需求

### F1 数据采集(混合降级链)

自动优先:ccusage(`npx ccusage@latest session --json`,零安装)→ 直接解析 `~/.claude/projects/*/*.jsonl`(token/模型/时间戳/**用户消息全文**——诊断的主要证据源)→ 会话内自查 + `/cost` 补充 → 问卷路径(≤6 问,首条 prompt 原文必收;诊断证据不足的项不输出)。缺数据维度记 N/A,禁止臆造。

### F2 能力诊断(核心功能)

十类信号,检测规则集中于 `references/capability-diagnosis.md`:

**思维架构类**:T1 问题定义(首条给方案、中途因需求错位推翻)/ T2 验收标准(说不出怎么算做对)/ T3 范围控制(会话中途 scope creep)/ T4 假设外显化(隐含前提导致返工)/ T5 抽象层级切换(细节死磕 vs 主动退一步)/ T6 委托校准(产品判断外包给 AI vs 机械活手工干)

**技术类**:J1 概念词汇缺口(绕圈描述有标准名的事物,缺词入台账并按领域聚类)/ J2 方案评估(对 AI 选型零质疑后架构级返工)/ J3 验证行为(不看 diff、bug 后置爆发)/ J4 知识沉淀(跨会话重复踩坑而未沉淀,仅跨会话检测)

诊断铁律:每条必须引用本次会话具体证据;措辞为假设并邀请确认;每次最多 2 条;弱证据不输出只记观察;禁止人格化措辞。

### F3 缺口台账与成长轨迹(gap ledger)

`gap-ledger.json` 按缺口类别累积**用户确认**的计数:确认 3 次 → 升级"结构性缺口",触发学习建议(方向 + 一个具体起点,不开长书单);被否决 2 次 → 停检该项(防误判模式);结构性缺口连续 5 次未出现 → 移入"毕业"并在报告中庆祝。词汇缺口同领域 ≥3 词时聚类,提示系统性补课方向。**看见成长与看见缺口同等重要。**

### F4 五维效率评分(证据基底)

token 效率、模型/模式选择、时长与迭代、提示词质量、委托校准,各 1–5 分,规则见 `references/scoring-rubric.md`。委托校准是效率与成长两套体系的交汇点,唯一既打分又入台账的维度。冷启动(<10 条记录)用启发式基准,≥10 条按同复杂度档位(S/M/L)切个人中位数基线。

### F5 报告(诊断为正文)

固定结构与篇幅比例:① 本次诊断(最大篇幅,1–2 条,证据 + 假设 + 追问)② 成长轨迹(台账动态:升级/毕业/观察中/词汇聚类)③ 效率评分卡(紧凑一表)④ 工作流建议(≤2 条,可省略)。

### F6 反馈闭环

诊断需用户确认/否决,结果决定台账计数;建议采纳记入 `learnings.md`,连续拒绝 ≥2 次的类别停提。

### F7 外源知识库与学习闭环(v2.1 新增)

内置精选知识库 `references/knowledge-base.md`:28 条起步,按诊断类别索引(非按来源),每条含可执行技巧本体、适用环境、来源三级分级(official > verified > blogger)与链接、**生效验证方式**、状态。质量门:可操作、有机制解释、不与官方冲突;拒收玄学咒语与不可验证宣称。

学习闭环:命中建议必须引用 KB 条目与出处;用户采纳 → 写入 tips_to_try(每次新增 ≤2 条);**下次复盘先验证上次实践**(tried-worked / tried-failed / pending;连续 2 次 pending 询问去留,不做第三次提醒)。

更新机制三来源:月度维护指令(固定信号源清单:Anthropic Engineering 与 Claude Code changelog、follow-builders 建造者摘要、Cursor/OpenAI/Vercel 官方;产出 diff 先经用户审核再写入);复盘临场搜索命中的好技巧(询问后收录);用户主动提名。距上次更新 >45 天时复盘中提醒。

## 四、成功标准

- **北极星:建议实践率 ≥50%**(tips_to_try 中 tried 状态占比;低于此值说明建议不够可执行或不够对症)
- 诊断确认率 ≥60%(信号规则的误报监控)
- 8 周内至少 1 个缺口走完"出现→结构性→毕业"全周期,或确认缺口的复现频率下降
- 复盘习惯不衰减(靠"每次最多 2 条诊断"与毕业庆祝机制维持)
- 单次复盘(自动路径)≤2 分钟出报告

## 五、风险

| 风险 | 应对 |
|---|---|
| 诊断误判伤害用户信任 | 假设式措辞 + 用户确认制 + 否决 2 次停检 + 弱证据不输出 |
| 只见缺口不见成长导致挫败 | 毕业机制 + 成长轨迹段固定呈现 |
| 问卷路径证据太薄 | 明示"贴多少诊断多少",不足不诊 |
| ccusage/日志 schema 变更 | 采集降级链;规则与采集分离 |
| 阈值不合个人节奏 | 全部集中在两个 reference 文件,直接改数字 |

## 六、里程碑

- v2.0(已完成):能力诊断 + 台账 + 五维评分,打包交付
- v2.1(实测 3–5 次后):按诊断确认率校准信号规则
- v2.2(台账运行 4 周后):验证升级/毕业机制;评估周报聚合视图
- 观察项:Stop hook 自动触发;将"毕业"能力反向沉淀为用户的个人 PM 方法论文档
