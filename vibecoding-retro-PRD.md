# PRD:vibecoding-retro

版本 v3.0 · 2026-08-04 · 状态:已实现

**v3.0 的核心变更是做减法。** v2.x 把产品做成了两个东西的叠加:一个对话效率审查器,和一个 PM 能力成长诊断系统。后者需要跨会话积累三五次才产出第一个结论,对一个别人也要装的工具是致命的——新用户第一次用,那套机制全是空的。而且它把内部分类学(T1/J4/KB-01/L 档)直接漏进了报告,连产品作者本人都看不懂。v3.0 删掉分类学与状态机,保留观察能力,输出改成大白话。

## 一、定位

**一句话**:复盘你和 AI 的每一段编程对话,用大白话告诉你哪里浪费了、为什么、下次怎么改。

**判断任何功能该不该做,只有一个标准**:它能不能让用户的下一次对话更省、更快、更少走弯路。

形态是 Claude Code plugin,**界面就是对话**。数据全在用户本地,无服务器、无网络请求。

**现有工具的断层**:ccusage / token-dashboard 只回答"我花了多少 token";cost-analysis 类做规则化的浪费检测但不解释因果、也不管提问方式。本产品把**逐轮因果归因**和**提问方式/思路的观察**接在一起,并且**用人话输出**。

## 二、目标与非目标

**目标**:每段对话结束说一句"复盘",两分钟内拿到一份看得懂、能照做的报告;每条问题都指得到具体轮次、算得出代价、给得出今天能做完的改法;每周能看到哪些问题在反复出现。

**非目标**:

- 团队对比与排行、实时监控、Web dashboard
- 心理分析或人格评判(只说行为)
- **产品管理辅导、职业发展建议**——超出"对话效率"范围的一律不给
- 把某次复盘的结论写进产品自身的文件(那是用户的个人数据)
- 需要用户回来更新状态才能运转的机制

## 三、功能

### F1 数据采集

流式解析 `~/.claude/projects/*/*.jsonl`(文件可达数十 MB,禁止整读)。降级链:脚本 → `ccusage` → 会话内自查 + `/cost` → 问卷(≤6 问,首条 prompt 原文必收)。缺数据的维度记 N/A,**禁止臆造**。

采集内容:token 四项、模型与实际推理档位、轮次、活跃时长、用户消息全文、**逐轮账单**、浪费模式、密钥扫描、格式自检。

### F2 逐轮账单(证据骨架)

按 `promptId` 把每次 API 请求、工具调用、token 消耗、上下文增长归到发起它的那句话上。**这是 v2.6 引入、v3.0 保留的核心能力**:它让报告能说"第 7 轮那句话触发了 23 次请求",而不是"这次浪费多"。

`cum_billed_input` 支持**反事实圈定**:把已发生且已被作废的实测消耗做减法圈出来。**这是实测数字相减,不是模拟**;"预计能省 $0.4"那种编出来的预测明令禁止。

### F3 浪费检测

机械可测的部分,判定不依赖语义:重复读同一文件、超大工具结果、上下文拖拽、缓存命中率、压缩次数、上下文持续高位。阈值集中在 `references/waste-patterns.md` 与脚本顶部。

### F4 提问方式与思路的观察

`references/observation-patterns.md`,九类常见问题,**全部用现象命名,没有编号**:规格来得太晚、前提没说出口、一个会话装太多事、在细节里死磕、人机分工错位、用描述代替术语、对方案零追问、不看改动就继续、同一个坑反复踩。

**这部分依赖语义理解,禁止用关键词匹配脚本替代。**

### F5 报告

铁律三条:**说人话**(报告里不许出现任何内部编号、档位缩写、分类学名词)、**每条 = 现象 + 代价 + 怎么改**(缺一样就不写)、**最多 3 条且证据指到具体轮次**。

模板与自查清单在 `templates/report-template.md`。好行为要写出来,不为凑数硬找问题。

### F6 改法手册

`references/playbook.md`,按**问题现象**索引,每条给一个今天能做完的动作和一个验证方式。引用时不写锚点名,直接说做法本身。加条目要过三关(可操作、有机制解释、能验证),**先给 diff 经用户确认**。

### F7 报告归档与周报

每次复盘的报告全文存进 `reports/`,这是周报唯一的素材来源。周报回答单次答不了的问题:精力实际去哪了、同一个问题在几个项目里重现。**周报只给下周一件事。**

配套 SessionEnd 钩子:**只往索引里追加一行**,不生成报告、不弹通知、不调模型。周报据此报出复盘覆盖率,**只陈述不催促**。

### F8 和个人历史比(可选,冷启动时静默)

`compare` 拿本次和用户自己同规模任务的历史中位数比,**不足 10 条不出数**——没有分母就没有超标,且报告里不解释为什么没有。`effect` 按日期切分做前后实测对比,每侧不足 3 条标 `reliable:false`。

**偏差只负责开启调查,不负责下结论。**

### F9 安全

密钥扫描覆盖用户消息与助手的思考/回复(思考过程明文写在本地日志里)。命中处在解析输出里替换为 `«已隐去:类型,长度»`,**任何输出都不回显密钥字符**。档案只存指标与一句话摘要,不存对话原文。

### F10 格式自检

产品读的是 Claude Code **未公开承诺的内部日志格式**。每次解析自检版本与关键字段,异常进 `degraded` 并要求报告如实说明。**错的数字比没有数字更危险。**

## 四、明确删掉的东西(以及为什么)

| 删掉 | 原因 |
|---|---|
| T1–T6 / J1–J4 编号,S/M/L 档位,KB-nn 编号 | 内部索引漏给了用户;产品作者本人都看不懂 |
| 缺口台账状态机(观察→确认 3 次→结构性→连续 5 次未现→毕业→否决 2 次停检) | 要积累三五次才产出第一个结论;通用用户到不了 |
| 待实践清单四态 + 建议实践率 | 要求用户回来更新状态,没人会做,结果只是制造愧疚 |
| 词汇台账与聚类 | 触发率极低(三周攒了 1 个词) |
| 五维评分卡 | "委托校准 4 分"对用户零信息量;硬指标已经够用 |
| 知识库的来源三级分级与月度更新协议 | 内容留下,分级和协议是维护包袱 |
| 内置的某条用户个人实践 | 一次复盘的结论不该并进要发给所有人的产品 |

代码量:`ledger.py` 702 → 500 行,references 411 → 353 行,且可读性大幅提升。

## 五、成功标准

- **用户看得懂**:报告里出现内部编号 = 缺陷,不是风格问题
- **单次就有用**:第一次用、档案为空时,报告的价值不打折
- 每条问题都指得到具体轮次、说得出代价
- 单次复盘(自动路径)≤2 分钟出报告
- 建议照做之后,`effect` 能测出实测指标的改善

## 六、风险

| 风险 | 应对 |
|---|---|
| 误判伤害信任 | 证据强制指到轮次;弱证据不输出;好行为也要写 |
| 日志格式变更导致静默失真 | 格式自检 + `degraded` 强制披露 |
| 报告又滑回术语堆砌 | 模板里放自查清单;SKILL.md 列了正反例对照表 |
| 范围重新蔓延回"人生辅导" | SKILL.md 边界段明写:看到自己在写这类建议就删掉 |
| 阈值不合个人节奏 | 集中在两个 reference 文件和两个脚本顶部,直接改数字 |

## 七、里程碑

- v2.5–v2.6(已完成):确定性脚本层、逐轮归因、指标可信度分级、格式自检、plugin 化
- **v3.0(已完成):做减法——删分类学与状态机,输出改大白话**
- 观察项:实际使用几次后校准浪费阈值与密钥扫描误报率;若 `compare` 长期用不上(积累太慢),考虑一并删掉
